import { Counter as CounterComponent } from "../features/counter/Counter";

export default function Counter() {
  return (
    <div>
      <h2>Counter</h2>
      <CounterComponent />
    </div>
  );
}
